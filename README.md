# AIRPORT INFORMATION FINDER MODULE

**Airport information finder** is a module which takes the airport code as input and returns an object which contains :

* NAME
* CITY
* COUNTRY
of the airport.

If the code is not valid it returns an error message.